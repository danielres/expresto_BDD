- state ||= :individual

- case state

  - when :individual
    = content_tag_for :div, user do
      %h2.name= user.name
      %h3.expressions
        = t(:expressions_added_by, :user => user.name)
      %ul.expressions= render user.expressions, :state => :list_item