%h2= t(:expressions).capitalize
%h3= t(:qty_expressions_in, :qty => @expressions_count, :language => t(Language.find_by_code(locale).name.downcase)).capitalize
%ul.expressions
  = render @expressions, :state => :list_item
= paginate @expressions
